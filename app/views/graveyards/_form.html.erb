<%= form_for(@graveyard) do |f| %>
  <% if @graveyard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@graveyard.errors.count, "error") %> prohibited this graveyard from being saved:</h2>

      <ul>
      <% @graveyard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
